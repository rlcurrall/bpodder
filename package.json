{
  "name": "bpodder",
  "version": "0.1.0",
  "description": "GPodder API v2 compatible sync server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "LOG_LEVEL=debug bun run --watch src/index.ts",
    "test": "bun test",
    "update-feeds": "bun run src/index.ts --update-feeds",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check"
  },
  "dependencies": {
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "oxfmt": "^0.35.0"
  }
}
